{
  "name": "simple-lib",
  "version": "1.0.0",
  "description": "A simple library",
  "repository": "https://github.com/oluSammy/sample-js-library",
  "author": "Samuel Olumorin",
  "scripts": {
    "compile": "rm -rf dist/lib && tsc && tsc --build tsconfig.es5.json"
  },
  "devDependencies": {
    "typescript": "^5.0.4"
  }
}
